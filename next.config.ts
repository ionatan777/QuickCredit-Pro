import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  devIndicators: {
    position: 'bottom-right', // Mueve el indicador a la esquina inferior derecha (menos visible)
  },
};

export default nextConfig;
